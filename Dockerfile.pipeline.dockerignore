.git
.venv
.dvc/cache
src/data
models
notebooks
tests
docs
*.log
.env
.claude
.agents
**/__pycache__
**/.embedding_cache
.pytest_cache
.ruff_cache
# NOTE: src/query-taxonomy is deliberately NOT excluded here. The root
# .dockerignore drops it so the serving image cannot carry it; this service
# imports query_taxonomy directly and must. BuildKit prefers this file for
# `-f Dockerfile.pipeline` builds.
